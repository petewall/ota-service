version: '3'
services:
  ota-service:
    image: petewall/ota-service
    restart: always
    ports:
      - "8266:8266"
    environment:
      - DATA_DIR=/data
      - PORT=8266
    volumes:
      - /Users/pwall/data/firmware:/data/firmware
    restart: always
